# --- Core SBOM tooling (Python-based) ---
cve-bin-tool>=3.2           # scan SPDX/CycloneDX SBOMs

# --- SBOM generation (Python ecosystems) ---
cyclonedx-bom>=3.13.0       # build CycloneDX from requirements/poetry.lock/etc.

# --- Helpers / utilities ---
packageurl-python>=0.14.7   # PURL parsing/normalization when needed
tomli>=2.0.1; python_version < "3.11"  # TOML support for pyproject/lock parsing on older Pythons

# --- Optional: graph export/rendering (your DOT/SVG workflow) ---
graphviz>=0.20.3            # Python bindings (needs system Graphviz installed)
# macOS system dep (install once): brew install graphviz

# --- Optional: SPDX helpers (validation/conversion) ---
spdx-tools>=0.8.3
requests 
tqdm
cve_searchsploit
semgrep
cve-bin-tool 
pandas
prettytable
toml
typer